"""
config.plugins

Default per-plugin YAML configuration, packaged as data so it can be
located via `importlib.resources.files("config") / "plugins"` after a
`pip install` — see `config/__init__.py`'s docstring.
"""
